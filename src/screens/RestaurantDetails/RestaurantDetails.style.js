import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  infoContainer: {
    width: '100%',
    alignItems: 'center',
  },
  additional: {
    width: '90%',
  },
});

export default styles;
